# Brain Age Prediction - Core Dependencies
# Edit this file, then run: pip-compile to generate requirements.txt

# ============ Deep Learning ============
# PyTorch 2.5.1 requires CUDA 12.1 or 12.4
# For CUDA 12.4: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# For CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
lightning>=2.5.0,<2.6.0              # PyTorch Lightning 2.5.x
torchmetrics>=1.9.0,<2.0.0         # Standard metrics (MAE, R2, Pearson)

# ============ Medical Imaging ============
monai==1.5.2                         # 3D medical imaging transforms, NIfTI I/O
nibabel==5.4.2                       # Neuroimaging file formats (NIfTI, CIFTI)
simpleitk>=2.5.0,<2.6.0              # Image registration and filtering

# ============ Data Science ============
numpy>=2.0.0,<2.4.4                  # NumPy 2.x (PyTorch 2.5 compatible)
pandas>=2.2.0,<3.0.0                 # Pandas 2.x stable (3.0 not released yet as stable)
scikit-learn>=1.5.0,<1.7.0           # Preprocessing, cross-validation, metrics
scipy>=1.14.0,<1.16.0                # Scientific computing
matplotlib>=3.10.8
seaborn>=0.13.2

# ============ Configuration & Logging ============
hydra-core>=1.3.2,<1.4.0             # YAML configuration management
omegaconf>=2.3.0,<2.4.0                # Structured configs for type safety
wandb>=0.25.0,<0.26.0                  # Experiment tracking
pyyaml>=6.0.0,<7.0.0                   # YAML parsing
rich>=13.0.0,<14.0.0                   # Beautiful console output
tensorboard>=2.20.0,<2.21.0            # Local logging backup
loguru>=0.7.0,<0.8.0                   # Modern logging

# ============ Utilities ============
tqdm>=4.66.0                           # Progress bars
python-dotenv>=1.0.0,<2.0.0            # Environment variable management
pyrootutils>=1.0.4,<2.0.0              # Project root resolution
packaging>=24.0                        # Version parsing