# Day  Log – July 30, 2026

**Today's goal:**
Finalize ABIDE data pipeline and start exploratory analysis. Set up the foundation for modeling (data loading, exploration, and planning for NeuroFusion implementation).

**Start Time:** 9.00 am
**End time:** 4.00 pm

---

## 1. Tasks Completed

- [x] Created ABIDEDataset and ABIDEDataModule classes in src/NeuroFusion/data_prep/abide_dataset.py.
  - Handles downloading of CC200 ROI time series via nilearn.

  - Filters to typical controls (DX_GROUP == 1) and age range 18–64.

  - Computes functional connectivity matrices (Pearson correlation) on the fly.

- [x] Resolved nilearn filtering issues (removed age_min/diagnosis parameters from fetch_abide_pcp; applied manual filtering).

- [x] Ran src/run.py to verify data loading – successfully loaded 123 subjects.
- [x] Created exploratory notebook notebooks/01_explore_abide.ipynb.
    - Visualised age, sex, site distributions.

    - Computed group-average connectivity and variability.

    - Analysed global connectivity vs. age (r ≈ -0.2, p ≈ 0.02).

    - Identified potential outliers (none severe).

- [x] Fixed import paths: updated run.py to use absolute imports (from NeuroFusion.data_prep...).
- [x] Resolved NumPy version conflict (downgraded to <2.0.0).


---
## 2. Experiments / Runs
- Run 1: python src/run.py

    - Status: Successful.

    - Output: Loaded 123 subjects, printed batch keys: ['age', 'fMRI', 'sub_id', 'site'].

    - Observations: Runtime warnings from np.corrcoef (division by zero) are benign – due to low‑variance ROIs. Can be suppressed.

---

## 3. Issues / Debugging
- Issue 1: NumPy 2.4.3 caused PyTorch import warning (_ARRAY_API not found).
Fix: Downgraded to numpy==1.26.4 (pinned in requirements).

- Issue 2: fetch_abide_pcp failed with KeyError: 'diagnosis' and 'age_min'.
Fix: Removed filtering arguments from the function call; applied manual filtering on the returned phenotypic DataFrame and ROI list.

- Issue 3: Import error ModuleNotFoundError: No module named 'src' in run.py.
Fix: Changed import to absolute from NeuroFusion.data_prep... (since src is not a package after installation).

---
## 5. Pre-commit & CI/CD
### Pre-commit Hooks
- [] Ran pre-commit run --all-files – resolved all issues after fixing notebook, updating Ruff config, and lint corrections.
- [] Need to add mypy stubs for nilearn and nibabel.

### CI/CD Pipeline (GitHub Actions)
Not yet set up – will add after baseline implementations are stable.






---

## 6. Today's Retrospective

- **What went well:**

  - Data pipeline is now robust and reuses cached data (no re‑download on repeated runs).

  - Exploration notebook provides clear insights into the dataset.

  - Successfully resolved import and dependency issues.-

- **What I struggled with:**
  - nilearn filtering API is tricky; manual filtering ended up being simpler.

  - NumPy version incompatibility with PyTorch wasted some time.

---
- **First action for tomorrow:**

  - Start implementing the NeuroFusion model with a dummy structural input (e.g., random noise) to test multi‑modal fusion logic.



---

## 7. Git Status


- **Current branch:** `feature/architecture`

- **Status:** Merged to `main`

- **Next branch:** `feature/module_neurofusion`

---
## 8. Next Steps (Day )
- [] implement NeuroFusion model – with a dummy structural input (random noise) to test the multi‑modal fusion logic.
- [] Write unit tests incrementally – for each module (ViT, GAT, fusion, full model).
- [] Write all baseline model classes:
  - CNN baselines (ResNet, EfficientNet, 3D-CNN) in `cnn_baselines.py`.
  - GNN baselines (GCN, BrainGNN, BC-GCN) in `gnn_baselines.py`.
  - Transformer baseline (MFFormer) in `transformer_baselines.py`.
  - Multimodal baselines (CTransformer, AMAge-Net) in `multimodal_baselines.py`.
- [] Set up GitHub Actions CI workflow 